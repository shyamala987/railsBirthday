<h1>Birthdays</h1>

<ul>
  <% @birthdays.each do |birthday| %>
    <li>
    <a href="<%= birthday_path(birthday)%>"><%= birthday.name  %></a> was born on <%= birthday.dob %>
    </li>
  <% end %>
</ul>

<%= link_to "Enter New Birthday", new_birthday_path %>

<p><%= link_to "Log out", logout_path %></p>