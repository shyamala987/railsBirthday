<h1>Birthdays</h1>

<ul>
  <% @birthdays.each do |birthday| %>
    <li>
    <a href="<%= birthday_path(birthday)%>"><%= birthday.name  %></a> was born on <%= birthday.birthday %>
    </li>
  <% end %>
</ul>

<h1>New Article</h1>
# <%= a href="<%= new_article_path %>">New Article</a>
